<h2>Users</h2>

<ul class="list-group">
  <% @users.each do |user| %>
    <%= link_to(user_path(user), class: "list-group-item list-group-item-action") do %>
        <%= render 'header', user: user, show_post_count: false %>
    <% end %>
  <% end %>
</ul>
